#!/bin/env bash

source colors

FONT="Operator Mono Medium:size=13"

# configuration of networkmanager-dmenu
networkmanager_dmenu -c -i -l 10 -nb "$bg" -nf "$fg" -sb "$altbg" -sf "$altfg" -fn "$FONT"
